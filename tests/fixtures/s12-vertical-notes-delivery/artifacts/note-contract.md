# Note contract

The bounded revision confirms one contract across the form, `POST /api/notes`, validation and the
atomic JSON store. 201 means persisted; 400 means invalid input; 500 means the prior store was
preserved. The external fake judge requested this one revision and no product scope was expanded.
